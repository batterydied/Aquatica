// Function to redirect user to a specific page
// __FIX_ME__USE CLASS
  
  // Event Listeners
  document.addEventListener("DOMContentLoaded", () => {
    const sellButton = document.querySelector(".sell");
    const saveListButton = document.querySelector(".save-list");
    const cartButton = document.querySelector(".cart");
    const profileButton = document.querySelector(".profile");
    const logoButton = document.querySelector(".logo-1");
  
    // sellButton.addEventListener("click", () => navigateTo("product.html"));
    // saveListButton.addEventListener("click", () => navigateTo("product.html"));
    // cartButton.addEventListener("click", () => navigateTo("product.html"));
    // profileButton.addEventListener("click", () => navigateTo("product.html"));
    // logoButton.addEventListener("click", () => navigateTo("product.html"));
  });
  